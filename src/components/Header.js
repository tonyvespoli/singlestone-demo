import { ReactComponent as Logo } from "../assets/logo-endless.svg";

const Header = () => (
  <header>
    <div className="content-container fade-in vertical-center">
      <Logo />
    </div>
  </header>
);

export default Header;
